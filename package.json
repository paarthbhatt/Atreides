{
  "name": "atreides",
  "private": true,
  "version": "0.1.0",
  "description": "Proof-carrying MCP security gateway",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@atreides/web",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "9.25.0",
    "eslint": "9.25.0",
    "globals": "16.4.0",
    "typescript-eslint": "8.64.0"
  },
  "engines": {
    "node": ">=22"
  }
}
